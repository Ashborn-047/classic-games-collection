[package]
name = "snake-arena"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "0.8" }
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
